---
title: What is Skylence?
description: Skylence is a Claude Code harness builder — it listens for GitHub webhook events, matches them to workflow definitions, and runs each step via the claude CLI, streaming results over WebSocket in real time.
unlisted: true
icon: lucide--sparkles
link: /faq/
---

Skylence is a Claude Code harness builder — it listens for GitHub webhook events, matches them to workflow definitions, and runs each step via the `claude` CLI, streaming results over WebSocket in real time.

[View full FAQ](/faq/)
